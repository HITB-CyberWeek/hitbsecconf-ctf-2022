version: '3'
services:
  crs:
    container_name: crs
    restart: unless-stopped
    build: .
    ports:
      - 4444:4444          # Service port. Public.
      - 127.0.0.1:2222:22  # SSH port. Private.
    volumes:
      - ./:/app
