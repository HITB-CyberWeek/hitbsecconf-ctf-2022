version: 1
service: issuecker
username: issuecker

scripts:
  build_outside_vm: |
    /home/issuecker/build_in_docker.sh
  start_once: |
    docker compose -f /home/issuecker/docker-compose.yaml up -d
files:
  - sources:
      - docker-compose.yaml
      - build
      - lighttpd
      - api.cpp
      - validators.cpp
      - hasher.cpp
      - session_manager.cpp
      - utils.cpp
    destination: /home/$USERNAME

proxies:
  - name: main
    listener:
      protocol: http
      certificate: wildcard.ctf.hitb.org
    upstream:
      host_index: 6
      port: 4444
    limits:
      - source: team
        location: /
        limit: 300r/m
        burst: 100
    dns_records:
      - issuecker
